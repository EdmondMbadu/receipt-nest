rules_version = '2';
// Firebase Storage Security Rules
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all users (you can customize this based on your needs)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}


